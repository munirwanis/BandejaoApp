@model IEnumerable<BandejaoApp.Models.CardapioModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DiaDaSemana)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DiaDaSemana)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CardapioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CardapioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CardapioId })
        </td>
    </tr>
}

</table>
