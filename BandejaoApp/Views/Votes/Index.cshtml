@model IEnumerable<BandejaoApp.Models.VotesModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CardapioItem.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vote)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CardapioItem.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vote)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VoteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VoteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VoteId })
        </td>
    </tr>
}

</table>
